# Base URL of the Patho backend API (see ../backend).
# The frontend still runs on mock data by default (see src/services/*) —
# this only matters once those services are wired up to real fetch calls.
VITE_API_BASE_URL=http://localhost:5000/api/v1
MONGODB_URL="mongodb+srv://<db_username>:pGMNPrOnkbmjiE1X@pathocluster.n8t0wnd.mongodb.net/?appName=pathoCluster"
